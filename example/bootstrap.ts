/**
 * Loaded once before the Vue app is created (`bootstrap` in site.config.ts).
 * Use for polyfills, one-time globals, etc.
 */
console.log('[vue-site example] bootstrap: ran (before createApp)')
export {}
